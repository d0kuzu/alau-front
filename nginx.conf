server {
    listen 80;
    server_name localhost;

    location /frontend/ {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /frontend/index.html;
    }

    # Перенаправление, если запрос пришел на корень контейнера
    location / {
        return 301 /frontend/;
    }
}

